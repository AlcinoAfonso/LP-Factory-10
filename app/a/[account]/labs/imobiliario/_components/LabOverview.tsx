export function LabOverview({ canWrite, canDelete }: { canWrite: boolean; canDelete: boolean }) { return <section className="rounded-xl border bg-white p-5 shadow-sm"><p className="text-sm font-medium text-gray-500">Laboratório experimental</p><h1 className="mt-1 text-2xl font-semibold">Laboratório imobiliário</h1><p className="mt-2 text-sm text-gray-600">Persistência operacional mínima para conteúdos e itens semanais. O laboratório permanece fora do core comercial oficial da LP Factory.</p><p className="mt-3 text-xs text-gray-500">Permissões: {canWrite ? "edição habilitada" : "somente leitura"}; exclusão {canDelete ? "habilitada" : "restrita a owner/admin"}.</p></section> }
