"use client";
import { EmptyState } from "@/components/ui/empty-state"; import { contentStatuses, type RealEstateLabContent } from "@/lib/real-estate-lab";
export function ContentList({ contents, canWrite, canDelete, setStatusAction, deleteAction }: { contents: RealEstateLabContent[]; canWrite: boolean; canDelete: boolean; setStatusAction: (fd:FormData)=>Promise<void>; deleteAction:(fd:FormData)=>Promise<void> }) { if (!contents.length) return <EmptyState title="Nenhum conteúdo" description="Crie o primeiro roteiro ou pauta do laboratório."/>; return <div className="space-y-3">{contents.map(c=><article key={c.id} className="rounded-xl border bg-white p-4 shadow-sm"><div className="flex flex-wrap justify-between gap-3"><div><h3 className="font-semibold">{c.title}</h3><p className="text-sm text-gray-600">{c.topic} · {c.channel} · {c.status}</p></div>{canWrite && <form action={setStatusAction} className="flex gap-2"><input type="hidden" name="id" value={c.id}/><select name="status" defaultValue={c.status} className="rounded border p-2 text-sm">{contentStatuses.map(s=><option key={s}>{s}</option>)}</select><button className="rounded border px-3 text-sm">Alterar</button></form>}</div>{c.body && <p className="mt-3 whitespace-pre-wrap text-sm text-gray-700">{c.body}</p>}{canDelete && <form action={deleteAction} className="mt-3"><input type="hidden" name="id" value={c.id}/><button className="text-sm text-red-700" formAction={deleteAction} onClick={(e)=>{if(!confirm('Excluir conteúdo?')) e.preventDefault()}}>Excluir</button></form>}</article>)}</div> }
