name: Pipeline Supabase — Inspect

on:
  workflow_dispatch:
    inputs:
      briefing:
        description: "Briefing curto (opcional). Para briefings longos, use briefing_path."
        required: false
        default: ""
      briefing_path:
        description: "Path de um arquivo .md/.txt no repo com o briefing (opcional)."
        required: false
        default: ""
      openai_model:
        description: "Modelo catalogado do workload"
        required: false
        type: choice
        options:
          - gpt-4.1-mini
        default: "gpt-4.1-mini"

permissions:
  contents: read

concurrency:
  group: pipeline-supabase-inspect-${{ github.ref }}
  cancel-in-progress: false

jobs:
  inspect:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: automations/supabase-inspect

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install deps
        run: npm ci

      - name: Check project
        run: npm run check

      - name: Run inspector
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SUPABASE_DB_URL_READONLY: ${{ secrets.SUPABASE_DB_URL_READONLY }}
          BRIEFING: ${{ inputs.briefing }}
          OPENAI_MODEL: ${{ inputs.openai_model }}
          OPENAI_COST_INGESTION_ENABLED: ${{ vars.OPENAI_COST_INGESTION_ENABLED }}
          OPENAI_COST_INGESTION_URL: ${{ vars.OPENAI_COST_INGESTION_URL }}
          OPENAI_COST_INGESTION_ENVIRONMENT: ${{ vars.OPENAI_COST_INGESTION_ENVIRONMENT }}
          OPENAI_COST_INGESTION_PROTOCOL_VERSION: ${{ vars.OPENAI_COST_INGESTION_PROTOCOL_VERSION }}
          OPENAI_COST_INGESTION_HMAC_SECRET: ${{ secrets.OPENAI_COST_INGESTION_HMAC_SECRET }}
        run: |
          if [ -n "${{ inputs.briefing_path }}" ]; then
            export BRIEFING_PATH="${{ github.workspace }}/${{ inputs.briefing_path }}"
          fi
          npm run start
