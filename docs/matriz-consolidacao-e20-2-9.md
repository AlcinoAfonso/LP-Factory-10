# Matriz de consolidação — E20.2.9

## 1. Referências imutáveis

- V1: PR #825, head `fb05fff258ff234a2bb57c1f4479b25297860d46`, merge commit `1c0c27782aed883cad7345851ffdbd1f3f471b65`, blob `3cce7a7570b4634b8d5cac61a5a48189302928b6`, path `docs/lousa-plano-base-e20-2-9.md`.
- Roadmap congelado: commit `1c0c27782aed883cad7345851ffdbd1f3f471b65`, blob `d5652c32b2617fbf5499a1d8ed1a901d234480fc`.
- V2 inicial: commit `abc6211b856721b84bf1e4cdd96f96f301f80142`, checkpoint `LP-Factory-Stage: plan-v2`.
- Plano conceitual: PR #822, head `a4b5c74e7b115dbce1b669a242499520b9103b5e`, `docs/matriz-debate-e19-5-4.md`, somente como origem das decisões humanas consolidadas na v1.
- Parecer Estrutural: agente `01a04409-610c-78d0-9e23-bf7947e2d59e`, conclusão `requer patch estrutural`.
- Parecer de Updates: agente `01a04409-6249-7321-8aa9-27ac6e9a9d72`, veredito `updates aplicáveis com patches autossuficientes`.
- Gestor de Automações: `N/A — nenhuma fase com Automação: sim`.

## 2. Achados e tratamentos

| Especialista | ID | Achado fiel | Classificação original | Relação com o escopo | Tratamento | Destino do update | Localização na v2 | Evidência ou justificativa |
|---|---|---|---|---|---|---|---|---|
| Gestor Estrutural | `GE-01` | Falta definir a compatibilidade do baseline histórico da identidade sem regravar snapshots. | Bloqueante com patch `PE-01`. | preservação | incorporado | N/A | 1.7 e 2.2 | A v2 projeta em memória o baseline v5 singular, falha fechada para legado malformado e fixa igualdade material sem persistir a projeção. |
| Gestor Estrutural | `GE-02` | A autoridade única do novo value type não estava materializada. | Bloqueante com patch `PE-02`. | extensão adjacente necessária e proporcional | incorporado | N/A | 2.1 — Processamento e Validação | A v2 concentra tipo, validação, canonicalização e igualdade material no boundary público do input catalog e proíbe reimplementação nos consumidores. |
| Gestor Estrutural | `GE-03` | Metadados estruturais e substituição dos caminhos vigentes estavam subespecificados. | Bloqueante com patch `PE-03`. | preservação | incorporado | N/A | 1.5, 1.6, 2.1 e 3.1 | A v2 fixa scope/origin/substitution/version, retirada forward-only e reuso de `sameCommercialWorkConfirmed`; fields antigos permanecem apenas em versões/readers históricos e nos adapters/bootstraps de compatibilidade até canonicalização, sem autoridade corrente. |
| Gestor Estrutural | `GE-04` | O banco vigente já suporta o caso; migration deve ser explicitamente excluída. | Bloqueante com patch `PE-04`. | preservação | incorporado | N/A | 1.7, 2.1 — Persistência e 4.2 | A v2 proíbe schema, migration, ACL e nova residência, reutilizando a residence/save E19.2 pré-handoff e as residences/RPC E19.5 vigentes; divergência operacional exige parada. |
| Gestor de Updates | `supa#40` | Reutilizar verificadores SQL versionados das residences E19.5 e do lifecycle E20.2.8. | Elegível atual, complementar. | preservação | incorporado | usar como referência, validação ou trava | 3.2 — Gates de publicação | A Etapa 2 exige os dois snippets read-only sem mutação e separa essa prova das regras semânticas de `offering_scope`. |
| Gestor de Updates | `vercel#15` | Ferramenta poderia apoiar QA visual de Preview, mas não acrescenta critério independente. | Elegível condicional, complementar. | expansão | não incorporado — benefício incremental não proporcional e disponibilidade não comprovada | não aplicável ao recorte | N/A | `prod#16` cobre o QA de forma independente de ferramenta; nenhum setting, dependência ou processo Vercel é criado. |
| Gestor de Updates | `prod#12` | Breadcrumb ou switcher contextual pode reduzir erro futuro de contexto entre múltiplas LPs/contas. | Elegível condicional, complementar. | expansão | incorporado somente como oportunidade condicional, sem implementação | preservar como oportunidade estratégica condicional | 2.4 | A v2 registra hipótese, complexidade média, gatilho objetivo e limite explícito; não cria navegação, grupo ou entidade. |
| Gestor de Updates | `prod#14` | Os três modos devem favorecer reconhecimento em vez de memorização dos valores técnicos. | Elegível atual, complementar. | extensão adjacente necessária e proporcional | incorporado | usar como referência, validação ou trava | 2.4 | A v2 exige rótulo humano e estado selecionado reconhecível para cada modo. |
| Gestor de Updates | `prod#16` | A superfície autenticada tocada exige QA proporcional em Preview. | Elegível atual, complementar. | extensão adjacente necessária e proporcional | incorporado | usar como referência, validação ou trava | 3.2 — Gates de publicação | O QA operacional permanece na Etapa 2; a Etapa 1 não cria UI ou save do cliente. |
| Gestor de Updates | `prod#17` | O novo controle composto exige labels, erros, foco e teclado acessíveis. | Elegível atual, complementar. | extensão adjacente necessária e proporcional | incorporado | usar como referência, validação ou trava | 2.4 e 3.1 | A v2 exige associação programática, teclado, foco, estado perceptível, alvos responsivos e proíbe alegar conformidade WCAG integral. |
| Orquestração | `AUTO-01` | A v1 e a única subseção executável registram `Automação: não`. | Não aplicável. | preservação | não incorporado — nenhuma avaliação formal exigida | N/A | 1.1 e 3.1 já preservadas | `Gestor de Automações: N/A — nenhuma fase com Automação: sim`. |
| Analista — Passagens 1 e 2 | `AN-01` | A residência e o fluxo E19.2 pré-handoff estavam omitidos da persistência. | Correção objetiva obrigatória. | preservação | incorporado | N/A | 1.7, 2.1, 2.3 e 3.1 | A v2 inclui `account_landing_page_onboarding_configurations`, save/revalidação E19.2, residences/RPC E19.5 e o mesmo `C`, sem nova residência ou migration. |
| Analista — Passagens 1 e 2 | `AN-02` | A restrição das referências legadas excluía adapters necessários à adaptação runtime. | Correção objetiva obrigatória. | preservação | incorporado | N/A | 1.6 e 1.7 | A v2 permite os fields singulares somente em adapters de compatibilidade, bootstraps legados, readers históricos e testes até canonicalização, sem autoridade corrente. |
| Analista — Passagens 1 e 2 | `AN-03` | O aceite não provava integralmente a transição E19.2 pré-handoff. | Correção objetiva obrigatória. | extensão adjacente necessária e proporcional | incorporado | N/A | 2.1 — Validação e 3.2 | A Etapa 1 antecipa somente a projeção read-only e a falha fechada necessárias ao gate; save/reload, handoff e igualdade de `C` ficam preservados como aceite obrigatório da Etapa 2. |
| Decisão humana vinculante | `HU-01` | Substituir `selected` por `multiple`; `offerings` é entrada livre e `business_offerings_summary` não é catálogo, whitelist nem fonte exaustiva. | Correção obrigatória superveniente. | preservação | incorporado | N/A | 1.4, 1.5, 2.1, 2.4 e 3.1 | Contrato, parser e testes usam `single | multiple | portfolio`, rejeitam `selected`, não comparam nem derivam o conteúdo factual e preservam unicidade case-insensitive após `trim`. |
| Decisão humana vinculante | `HU-02` | O bootstrap precisa incluir a compatibilidade read-only usada pelo gate E20.2.8, mantendo `CURRENT=5` e toda operação do cliente em v5. | Correção obrigatória superveniente. | extensão adjacente necessária e proporcional | incorporado | N/A | 1.7, 2.1, 2.3 e 3.1 | A Etapa 1 projeta em memória os dois fields legados somente contra registry candidato, canonicaliza, falha fechada e cobre o contador de configurações inválidas. |
| Decisão humana vinculante | `HU-03` | Replanejar em duas etapas: bootstrap primeiro; draft/revisão/publicação v6 somente após o suporte chegar à `main`. | Correção obrigatória superveniente. | preservação | incorporado | N/A | 1.1, 1.7, 2.3, 3.1 e 3.2 | O PR #826 passa a conter apenas E20.2.9.1; registry v6, `CURRENT=6`, identidade E19.5, UI operacional, geração e snapshots ficam na E20.2.9.2. |
| Decisão humana vinculante | `HU-04` | Preview e Production compartilham o mesmo Supabase; o HEAD v6 exige `R=6` para a transição material, enquanto a reconciliação canônica é exclusiva de Production. | Correção obrigatória do gate hospedado. | preservação | incorporado | N/A | 3.2 — Gates de publicação | O Preview exato do artefato v6 fica aprovado como gate de build/artefato. O QA operacional completo não foi dispensado: foi transferido para imediatamente após a reconciliação canônica em Production, sem bypass de Preview, escrita antecipada em `reviewed_input_catalog_version` ou mudança do contrato do workspace. |
| Decisão humana vinculante | `HU-05` | Após a publicação da v6, a reconciliação falhou fechada porque a evidência preservada usava fingerprint legado sensível à ordem das propriedades, embora draft e registry implantado representassem o mesmo contexto material. | Correção pós-merge obrigatória. | preservação | incorporado | N/A | 3.2 — Gates de publicação | A E20.6 passa a expor o fingerprint canônico focal já alinhado à igualdade material. Novas evidências usam essa autoridade; a evidência v6 só é aceita quando o fingerprint legado armazenado é reproduzido exatamente a partir do draft preservado, o contexto implantado é materialmente igual e conteúdo, taxon, cadeia, pesquisa e versão permanecem idênticos. Falha em qualquer prova bloqueia sem escrita, sem nova IA, decisão humana ou alteração manual do estado. |
| QA pós-reconciliação | `QA-01` | A reconciliação canônica foi concluída em Production após o corretivo de fingerprint, com `reviewed_input_catalog_version=6`, leitura final positiva e remoção do draft; no QA seguinte, um `invalid_values` de `landing_page_offering_scope` preservou apenas o valor inválido visível, enquanto selects válidos e revisões hidden voltaram aos defaults do formulário. | Correção pós-merge obrigatória. | preservação | incorporado neste corretivo focal | N/A | 2.4 e 3.2 — Gates de publicação | O diagnóstico isolou o reset nativo agendado pela Form Action do React, sem remontagem ou refresh no caminho inválido. Somente erros de campo corrigíveis reidratam valores e revisões submetidos; conflitos de revisão e falhas de autoridade continuam sem estado reutilizável. O foco de `landing_page_offering_scope` passa ao textarea `onboarding-landing_page_offering_scope-offerings`, e as regressões provam erro sem write seguido de correção e save válido. |
| Dependência pós-`QA-01` | `QA-02` | No reteste após o #838, a correção para o baseline persistido produziu `success + intent=next` sem incremento de `revision` ou `sharedRevision`, mas o componente confundiu a revisão preservada com a identidade de uma conclusão já processada. | Corretivo pós-merge focal da E19.5.3. | dependência externa | não incorporado à E20.2.9 | N/A | 3.2 — Gates de publicação | A autoria funcional foi classificada como E19.5.3 específica: o save idempotente/no-op é válido e deve continuar sem write ou nova revisão. A E20.2.9 não amplia plano ou implementação; seu QA aguarda o corretivo E19.5.3 que identifica cada novo resultado da Form Action independentemente da revisão e o processa uma única vez. |
| Decisão humana vinculante | `HU-06` | A lista de ofertas passa a determinar `single/multiple`; somente a declaração de portfólio permanece explícita, substituindo os rádios previstos originalmente. | Ajuste focal superveniente da UX E20.2.9. | preservação | incorporado no controle existente, sujeito ao QA Preview | N/A | 2.4 | `OfferingScopeControl` deriva o modo pela quantidade e pela declaração humana de portfólio, sem alterar shape, parser, igualdade material, identidade E19.5 ou consumidor E20.7. O gate exige transições, validação de duplicidade, preservação #838/#841, save/reload, desktop/mobile e teclado; não cria v7 nem infraestrutura. |

## 3. Gate do Analista

- Passagem 1: agente `01a04426-ee8d-7d91-a616-b1d7e1c943c6`, modo `passagem_independente`, sem acesso aos pareceres ou a esta matriz.
- Conclusão da Passagem 1: `aprovado com correções obrigatórias`.
- Correções objetivas identificadas: incluir residência e fluxo E19.2 pré-handoff; preservar referências legadas nos adapters de compatibilidade até canonicalização; provar leitura, projeção, save/reload, handoff lazy, falha fechada e igualdade do mesmo `C` entre E19.2, workspace e geração.
- Passagem 2: `aprovado com correções obrigatórias`, confirmando as três correções objetivas e exigindo sua rastreabilidade na tabela principal.
- Estado anterior: o plano havia sido aprovado antes da descoberta do bloqueio de bootstrap.
- Revisão de implementação da Etapa 1: agente `01a044f1-f6aa-7b03-aee9-45fb56f6245b`, veredito inicial `aprovado com correções obrigatórias` no HEAD `1b23f7e7243209f0ca37cf0a4be40361b231b036`.
- Revisão delta da implementação: mesmo agente, delta `1b23f7e7243209f0ca37cf0a4be40361b231b036..acb6439266bd7c4dc13ecbc57bba5ef1c5bd09cb`, veredito `aprovado para avançar`; as correções restringiram a projeção ao registry candidato completo/compatível e ampliaram as regressões do boundary.
- Revisão delta focal de unicidade: o mesmo agente aprovou para avançar o delta `acb6439266bd7c4dc13ecbc57bba5ef1c5bd09cb..7a7546030fba042f9c8749b8195cb6e6a41fa310` no HEAD `7a7546030fba042f9c8749b8195cb6e6a41fa310`, sem correções ou teste humano adicional.
- Estado pós-publicação: o PR #836 removeu o bloqueio de fingerprint e a reconciliação canônica concluiu em Production, avançando `R` para `6` e removendo o draft somente após a leitura final positiva.
- Gate vigente: os corretivos #838 e #841 estão incorporados à `main`; seus contratos permanecem preservados. O ajuste humano superveniente `HU-06` muda somente a obtenção do modo na UI existente e exige validações e QA em Preview antes da avaliação final. A reconciliação concluída não será revertida; a matriz e o histórico de `HU-05`, `QA-01` e `QA-02` permanecem preservados.
