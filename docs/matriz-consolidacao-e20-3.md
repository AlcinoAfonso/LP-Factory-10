# Matriz de consolidação — E20.3

- Caso: `E20.3 — Perfil de orientação para geração`.
- V1 congelada: PR #640, head `8e7dabd8eff48f16c1bce84cc3ad7624b091d8c7`, blob `54a5bc2afc17b3c803138d36b5bb64034f827319`.
- V2 corrigida: commit `2ff665c0bfc0ad7264d866d84d3b589edfe1f174`, blob `65681b14302d2d9ac95be3b7f2e08c53b529250b`.
- Gestor de Automações: `N/A`; nenhuma fase da v1 está marcada como `Automação: sim`.

| Especialista | ID | Achado fiel ou referência inequívoca | Classificação original | Tratamento | Seção ou trecho exato na v2 | Evidência ou justificativa |
| --- | --- | --- | --- | --- | --- | --- |
| Gestor Estrutural | `GE-E20.3-01` | A E18.5 não possui operação pública para validar somente módulo/versão e variante opcional/versão sem fabricar raiz ou perfil de funil. | `bloqueado por decisão humana` | `incorporado` | 1.2, 1.3, 1.4, 2.5, 3.2, 4.1 e 4.2 | Decisão humana de 26/07/2026 autorizou `validateLandingPageModuleIdentity` dentro da E20.3.4 e do mesmo PR, limitada à identidade/versionamento e vínculo, reutilizando o registry e proibindo resolver paralelo ou ampliação. |
| Gestor Estrutural | `GE-E20.3-02` | Segurança de banco não especificava schema, revogações, grant mínimo, ausência de policies nem independência entre RLS e GRANT. | `requer patch estrutural` | `incorporado` | 2.3 e critérios de aceite de 3.1 | A v2 fixa tabelas em `public`, RLS sem policies, `REVOKE ALL` de `PUBLIC`/`anon`/`authenticated`, revogação condicional de `ai_readonly`, `GRANT SELECT` exclusivo a `service_role`, ausência de grants mutáveis, uso de `createServiceClient()` e verificação das roles. |
| Gestor Estrutural | `GE-E20.3-03` | Invariantes do agregado não estavam ligadas a constraints, ações de FK, timestamps, auditoria e Trigger Hub inequívocos. | `requer patch estrutural` | `incorporado` | 2.3 e critérios de aceite de 3.1 | A v2 materializa PKs, FKs com ações explícitas, checks, unicidades, índice parcial de ativa, par opcional de variante, textos não vazios, timestamps e limites de auditoria/lifecycle. |
| Gestor Estrutural | `GE-E20.3-04` | Boundary público, cadeia taxonômica, resolver puro e adapter não estavam separados até o nível necessário. | `requer patch estrutural` | `incorporado` | 2.1, 2.6, 2.7 e entregas/critérios de 3.1 e 3.2 | A v2 fixa domínio puro em `generation-profile/`, adapter server-only em path canônico, entrada pública somente por `taxonId`, DTO e invariantes da cadeia interna, falhas tipadas, preservação dos exports existentes e API agregada mínima. |
| Gestor Estrutural | `GE-E20.3-05` | Validação e documentação não previam script dedicado, banco isolado, provas de segurança, Base Técnica nem proteção pré-apply. | `requer patch estrutural` | `incorporado` | 2.3 e entregas/critérios de 3.1 e 3.2 | A v2 exige script dedicado, reconstrução isolada, casos SQL, `docs/base-tecnica.md`, `docs/schema.md`, gate explícito sem consumidor/rota/call path runtime antes do apply e da verificação pós-merge e validações explícitas. |
| Gestor Estrutural | `GE-E20.3-06` | O recorte evita a arquitetura abandonada e preserva duas tabelas, leitura server-side e ausência de UI, automação e lifecycle mutável. | `achado favorável sem patch` | `não incorporado — justificado` | `N/A` | Não exigia alteração. A v2 preserva o escopo negativo da v1 e proíbe reaproveitamento automático dos objetos do PR #631. |
| Gestor de Updates | `GU-supa#35` | Index Advisor possui relação complementar com consultas futuras, sem evidência atual de lentidão ou plano inadequado. | `complementar`; horizonte `condicional`; rejeitado | `não incorporado — justificado` | `N/A` | Tuning sem query material ou hipótese mensurável seria especulativo; índices decorrem das consultas e invariantes já conhecidos. |
| Gestor de Updates | `GU-supa#40` | Snippet SQL local read-only oferece evidência reproduzível das duas tabelas, constraints, RLS, grants e ausência de registros. | `complementar`; horizonte `atual`; aprovado | `incorporado` | Entregas e critérios de aceite de 3.1 | A v2 exige `supabase/snippets/e20_3_generation_profile_verify.sql` estritamente read-only, incluindo a verificação condicional de `ai_readonly`, e sua execução pós-merge/apply, sem substituir a migration ou autorizar mutação antecipada. |
| Gestor de Updates | `GU-supa#41` | Supabase Assistant poderia apoiar investigação futura do adapter, mas não há query lenta. | `complementar`; horizonte `condicional`; rejeitado | `não incorporado — justificado` | `N/A` | Não existe hipótese de performance; sobrepõe-se ao Index Advisor e não integra o gate atual. |
| Gestor de Updates | `GU-supa#57` | Schema Visualiser pode mostrar as tabelas e FKs, mas não prova checks, índice parcial, RLS, grants ou ausência de dados. | `complementar`; horizonte `atual`; rejeitado | `não incorporado — justificado` | `N/A` | É redundante e menos determinístico que o snippet SQL aprovado. |
| Gestor de Updates | `GU-supa#63` | `rlsautotest` tem relação com RLS, mas é comunitário, beta e adicionaria tooling desproporcional. | `complementar`; horizonte `condicional`; rejeitado | `não incorporado — justificado` | `N/A` | SQL explícito é suficiente para duas tabelas internas; não instalar pgTAP, Python ou workflow novo. |
| Gestor de Updates | `GU-prod#6` | Conteúdo para busca generativa tem relação futura com `generation_guidance`, mas a E20.3 não cria perfil oficial nem gera conteúdo. | `complementar`; horizonte `futuro`; rejeitado | `não incorporado — justificado` | `N/A` | Reavaliar no plano futuro de cadastro/consumo real; incluir agora ampliaria o contrato de persistência. |

## Travas preservadas

- O update `supa#40` permanece validação read-only e não autoriza apply antes do merge.
- Nenhum update rejeitado vira dependência, ferramenta ou gate deste recorte.
- A decisão humana sobre `GE-E20.3-01` não autoriza rota, banco, serviço, catálogo, contexto artificial, refatoração ampla ou resolver paralelo na E18.5.
- A matriz permanece disponível até a declaração de entrega completa do Executor.
